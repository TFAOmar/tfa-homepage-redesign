## Goal
Verify Patricia Serafin's advisor form submission delivers:
1. Email to `patricia@tfainsuranceadvisors.com`
2. Copy to `leads@tfainsuranceadvisors.com` (with "DO NOT CONTACT — generated by Patricia Serafin" banner)
3. **No** Pipedrive person/lead created (skipped as expected)

## Test Steps

1. **Identify a Patricia form entry point** — locate her advisor landing page(s) in `src/pages/` and confirm the submit path calls `pipedrive-submit` with `advisor_name: "Patricia Serafin"` and `advisor_email: "patricia@tfainsuranceadvisors.com"`.

2. **Drive submission via Playwright** against `http://localhost:8080`:
   - Navigate to Patricia's landing page
   - Fill form with test data (name: "Test Lead - Patricia", unique email, phone, select interests)
   - Submit and capture screenshots of success state
   - Capture network response from `pipedrive-submit`

3. **Verify edge function behavior** via `supabase--edge_function_logs` for `pipedrive-submit`:
   - Confirm log line `[Pipedrive Skipped] Advisor-specific form` appears (no person/lead created)
   - Confirm both emails dispatched (advisor + team)
   - Confirm team email HTML includes the red "DO NOT CONTACT" banner referencing Patricia

4. **Verify DB record** via `supabase--read_query` on the form submission table to confirm the row was written with advisor attribution.

5. **Report results** with screenshot evidence, log excerpts, and pass/fail per criterion.

## Notes
- No code changes — this is a verification-only run.
- If any criterion fails, I'll surface the exact log/response and propose a fix in a follow-up plan.
